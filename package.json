{
  "name": "my-raydium-wrapper", 
  "version": "1.0.0",
  "description": "A wrapper for the Raydium SDK",
  "main": "dist/index.js",
  "types": "dist/index.d.ts", 
  "scripts": {
    "build": "tsc && tsup src/index.ts --format cjs,esm --dts --sourcemap --no-splitting --minify -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/imranMjbCro/raydium-sdk-V2.git" 
  },
  "author": "imranMjbCro",
  "license": "MIT",
  "devDependencies": {
    "tsup": "^6.7.0", 
    "typescript": "^5.0.4" /
  },
  "dependencies": {
    "@solana/web3.js": "^1.88.0", 
    "@raydium-io/raydium-sdk-v2": "^0.1.35-alpha" 
  }
}
