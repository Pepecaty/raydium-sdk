{
  "name": "my-raydium-wrapper", // Choose a unique name for your package
  "version": "1.0.0",
  "description": "A wrapper for the Raydium SDK",
  "main": "dist/index.js", // Where your main entry point will be
  "types": "dist/index.d.ts", // Where your type definitions will be
  "scripts": {
    "build": "tsc && tsup src/index.ts --format cjs,esm --dts --sourcemap --no-splitting --minify -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/your-username/my-raydium-wrapper.git" // Replace with your repository URL
  },
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "tsup": "^6.7.0", // Or the latest version of tsup
    "typescript": "^5.0.4" // Or the latest version of TypeScript
  },
  "dependencies": {
    "@solana/web3.js": "^1.88.0", // Or your desired version of @solana/web3.js
    "@raydium-io/raydium-sdk-v2": "^x.x.x" // The Raydium SDK version you want to use
  }
}
